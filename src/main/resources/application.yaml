server:
  port: 9011
  
spring: 
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/daily101?useUnicode=true&charaterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: spring
    password: spring
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

